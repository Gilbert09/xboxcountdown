<%= render 'header' %>

<%= render 'sidebar' %>

<div id="container">
	<div id="table" class="box">
		<% @games.each do |game| %> 
			<div class="row">
				<div class="title">
					<span><%= game.game.title %></span>
				</div>
				<div class="console">
					<span><%= game.game.console %></span>
				</div>
				<div class="date">
					<span><%= game.game.release_date.strftime('%d/%m/%Y') %></span>
				</div>
				<div class="edit">
					<span><%= link_to "View", dashboard_game_view_path(game) %></span>
				</div>
				<div class="edit">
					<span><%= link_to "Accept", dashboard_game_queue_accept_path(game) %></span>
				</div>
			</div>
		<% end %>
	</div>
</div>

